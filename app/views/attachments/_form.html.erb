<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :attachment_user_id %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file, id: :attachment_file %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :attachment_name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type, id: :attachment_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
